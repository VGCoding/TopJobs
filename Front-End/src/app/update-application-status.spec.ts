import { UpdateApplicationStatus } from './update-application-status';

describe('UpdateApplicationStatus', () => {
  it('should create an instance', () => {
    expect(new UpdateApplicationStatus()).toBeTruthy();
  });
});
